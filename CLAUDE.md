# Use this file to inject per-agent memory into a session. It is loaded automatically on every invocation.

# MCP Tools (Model Context Protocol entry points)
## SMARTPROXY
- [[SMARTPROXY]] is the primary MCP tool that routes all external API requests. Its URL is https://mcp.smartproxy.com. To use it, invoke the #smartproxy MCP through the MCP client.

## web_search
- [[web_search]] is the primary search MCP tool, usable only when web monitoring confirms it is active.

# Change Log
- 2026-06-03: Converted to per-agent MEMORY.md format following Claude Code's 2.0 compact.
